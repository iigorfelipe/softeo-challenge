import { UserProps } from '../../../types/UsersType';
import * as C from '../styles';

const UserDetailsHeader = ({ user }: UserProps) => {
  return (
    <C.Header>
      avatar e name
    </C.Header>
  )
};

export default UserDetailsHeader;
